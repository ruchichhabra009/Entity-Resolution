Question 1:

download the zip file and unzipped the folder
use terminal to get into the folder
install enchant for python3
install nltk

run python3 cleaned.py class.txt
the generated file will be cleaned.txt will contain the key-value pair corresponding to each professor as key
run python3 query.py cleaned.txt
the generated file cleaned_corrected will consist of key-value pair with corrected spell

Along with the file, the results of all the queries will be displayed on the terminal

Question 2:

install beautifulsoup
run transform.py
the result.csv will be generated which will contain the required result 